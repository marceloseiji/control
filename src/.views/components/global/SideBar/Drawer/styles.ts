import styled from "styled-components/macro";
import { Drawer as DrawerS } from "@material-ui/core";

export const Drawer = styled(DrawerS)`
  .MuiDrawer-paper {
    top: inherit;
    width: 240px;
  }
`;
